<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.code.review.mapper.MergeRequestMapper">

    <select id="selectMRLastCommitIdCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM mr_review_log
        WHERE project_name = #{projectName}
          AND source_branch = #{sourceBranch}
          AND target_branch = #{targetBranch}
          AND last_commit_id = #{lastCommitId}
    </select>

    <insert id="insertMergeRequest">
        INSERT INTO mr_review_log (project_name,
                                   author,
                                   source_branch,
                                   target_branch,
                                   updated_at,
                                   commit_messages,
                                   score,
                                   url,
                                   review_result,
                                   additions,
                                   deletions,
                                   last_commit_id)
        VALUES (#{projectName},
                #{author},
                #{sourceBranch},
                #{targetBranch},
                #{updatedAt},
                #{commitMessages},
                #{score},
                #{url},
                #{reviewResult},
                #{additions},
                #{deletions},
                #{lastCommitId})
    </insert>
</mapper>